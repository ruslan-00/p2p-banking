## Environment example for p2p-banking
# Path to SQLite database file (avoid committing this file to git)
DB_PATH=ledger.db
# Port the server will listen on
PORT=3000
# Node environment
NODE_ENV=development
